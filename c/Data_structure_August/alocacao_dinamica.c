#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main()
{	

	// REAOCAÇÃO USANDO A FUNÇAÕ REALLOC()
	/*char *frase;
	frase = (char *) malloc(6);
	
	strcpy(frase, "teste");
	
	// realocaçãõ da frase na memoria
	frase = (char *) realloc(frase, 20);
	strcpy(frase, "teste estrutura");	// Os valores anteriores serão substituidos	
	printf("%s", frase);
	*/
	
}

